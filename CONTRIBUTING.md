# دليل المساهمة

شكراً لاهتمامك بالمساهمة في مشروع جمعية! نرحب بجميع المساهمات سواء كانت:

- الإبلاغ عن الأخطاء (Bug Reports)
- اقتراح ميزات جديدة
- تحسين التوثيق
- إرسال Pull Requests

## 🚀 كيفية المساهمة

### 1. الإبلاغ عن الأخطاء

عند الإبلاغ عن خطأ، يرجى تضمين:

- وصف واضح للمشكلة
- خطوات إعادة إنتاج الخطأ
- السلوك المتوقع vs السلوك الفعلي
- لقطات شاشة (إن أمكن)
- معلومات الجهاز (نوع المتصفح، نظام التشغيل)

### 2. اقتراح ميزات جديدة

عند اقتراح ميزة جديدة:

- وصف واضح للميزة
- الفائدة المتوقعة للمستخدمين
- أمثلة على الاستخدام
- أي اعتبارات تقنية

### 3. إرسال Pull Requests

#### خطوات العمل

1. **Fork المستودع**
   ```bash
   git clone https://github.com/your-username/jamiya-app.git
   cd jamiya-app
   ```

2. **إنشاء فرع جديد**
   ```bash
   git checkout -b feature/اسم-الميزة
   # أو
   git checkout -b fix/وصف-الإصلاح
   ```

3. **تثبيت التبعيات**
   ```bash
   npm install
   ```

4. **إجراء التغييرات**
   - اتبع دليل الأسلوب أدناه
   - تأكد من عدم وجود أخطاء linting
   - اختبر التغييرات محلياً

5. **Commit التغييرات**
   ```bash
   git add .
   git commit -m "نوع: وصف موجز للتغيير"
   ```

   أنواع الـ Commit:
   - `feat:` ميزة جديدة
   - `fix:` إصلاح خطأ
   - `docs:` تغييرات في التوثيق
   - `style:` تغييرات في التنسيق (لا تؤثر على الكود)
   - `refactor:` إعادة هيكلة الكود
   - `test:` إضافة/تعديل اختبارات
   - `chore:` مهام صيانة

6. **Push إلى الفرع**
   ```bash
   git push origin feature/اسم-الميزة
   ```

7. **فتح Pull Request**
   - اذهب إلى صفحة المستودع
   - اضغط على "New Pull Request"
   - املأ النموذج بالتفاصيل
   - انتظر المراجعة

## 📝 دليل الأسلوب

### TypeScript

- استخدم TypeScript لجميع الملفات الجديدة
- حدد الأنواع بشكل صريح
- تجنب استخدام `any`

```typescript
// ✅ جيد
interface UserProps {
  name: string;
  age: number;
}

const User: React.FC<UserProps> = ({ name, age }) => {
  return <div>{name} - {age}</div>;
};

// ❌ تجنب
const User = ({ name, age }) => {
  return <div>{name} - {age}</div>;
};
```

### React

- استخدم Functional Components
- استخدم Hooks بدلاً من Classes
- تجنب استخدام `useEffect` بشكل مفرط

```tsx
// ✅ جيد
import { useState, useCallback } from 'react';

const Counter = () => {
  const [count, setCount] = useState(0);
  
  const increment = useCallback(() => {
    setCount(c => c + 1);
  }, []);

  return (
    <button onClick={increment}>
      العدد: {count}
    </button>
  );
};
```

### CSS/Tailwind

- استخدم Tailwind CSS للتنسيق
- اتبع نظام التصميم الموجود
- تجنب الكتابة المباشرة للـ CSS

```tsx
// ✅ جيد
<button className="bg-[#1B5E20] text-white px-4 py-2 rounded-xl">
  زر
</button>

// ❌ تجنب
<button style={{ backgroundColor: '#1B5E20', color: 'white' }}>
  زر
</button>
```

### التسمية

- المكونات: PascalCase (مثل: `UserCard`)
- الدوال: camelCase (مثل: `handleSubmit`)
- الثوابت: UPPER_SNAKE_CASE (مثل: `MAX_RETRY_COUNT`)
- الملفات: PascalCase للمكونات، camelCase للدوال

### اللغة العربية

- استخدم اللغة العربية في واجهة المستخدم
- استخدم الخط Tajawal
- تأكد من دعم RTL بشكل صحيح

## 🧪 الاختبار

### قبل إرسال PR

1. **تشغيل الاختبارات**
   ```bash
   npm run test
   ```

2. **التحقق من الأخطاء**
   ```bash
   npm run lint
   ```

3. **بناء المشروع**
   ```bash
   npm run build
   ```

4. **اختبار يدوي**
   - اختبر على متصفحات مختلفة
   - اختبر على أحجام شاشات مختلفة
   - تأكد من عمل RTL بشكل صحيح

## 📋 قائمة المراجعة

قبل إرسال Pull Request، تأكد من:

- [ ] الكود يعمل بشكل صحيح
- [ ] لا توجد أخطاء TypeScript
- [ ] لا توجد أخطاء ESLint
- [ ] التغييرات موثقة
- [ ] الـ Commit messages واضحة
- [ ] الفرع محدث مع `main`

## 🤝 قواعد السلوك

- كن محترماً ومهذباً
- تقبل النقد البنّاء
- ركّز على ما هو الأفضل للمجتمع
- احترم وجهات النظر المختلفة

## 📞 التواصل

- GitHub Issues: للأخطاء والميزات
- Discussions: للأسئلة والنقاشات
- Email: dev@jamiya.dz

شكراً لمساهمتك! 🎉
